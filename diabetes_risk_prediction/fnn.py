

# def FNN(Classifier):
	#  def __init__(self, input_dim, hidden_units=[64, 32], dropout_rate=0.5):
	# 	  super().__init__()
	# 	  self.model = self.build_model(input_dim, hidden_units, dropout_rate)